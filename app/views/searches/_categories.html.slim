#search-header-categories.search__categories
  - categories.each do |category|
    = link_to search_path(category: category[1]), class: ["filter-chip", ("active" if selected_category == category[1])].compact do
      = category[0]

.performance id=#{dom_id(performance)}
  div
    strong = t("performance_date", date: performance.date)
  div
    strong = t("performance_videos_count", videos_count: performance.videos_count)
  .performance__videos
    - performance.videos.each do |video|
      = render partial: "videos/video", locals: { video: video }

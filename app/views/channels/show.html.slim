p style="color: green"
  = notice
= render @channel
div
  = link_to t("edit_link"), edit_channel_path(@channel) |
  = link_to t("back"), channels_path
  = button_to t("destroy_button"), @channel, method: :delete

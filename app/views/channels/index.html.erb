<p style="color: green"><%= notice %></p>
<h1>Channels</h1>
<div id="channels">
  <% @channels.each do |channel| %>
    <%= render channel %>
  <% end %>
</div>
<%= link_to "New channel", new_channel_path %>

p = t(".greeting", email: @resource.email)

p = t(".your_account_has_been_locked")

p = t(".click_link_to_unlock")

p = link_to t(".unlock_my_account_link"), unlock_url(@resource, unlock_token: @token)

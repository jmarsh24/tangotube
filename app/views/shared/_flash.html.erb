<% flash.each do |key, value| %>
  <div class="flash">
    <sl-alert open variant="primary" duration="3000" closable>
      <sl-icon slot="icon" name="info-circle"></sl-icon>
      <%= value.html_safe %>
    </sl-alert>
  </div>
<% end %>
